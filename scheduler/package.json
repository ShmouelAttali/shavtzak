{
  "name": "shavtzak-scheduler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Automatic shift-schedule generator (see SPEC.md)",
  "scripts": {
    "generate": "tsx src/cli.ts generate",
    "validate": "tsx src/cli.ts validate",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test --test-concurrency=1 tests/*.test.ts"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
